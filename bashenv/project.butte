#!/bin/bash
PS3='Please enter session choice: '
options=("lriot")
select opt in "${options[@]}"
do
  case $opt in
      "lriot")
          export MJM_SESSION=$opt
          break
          ;;
      *)
          export MJM_SESSION="lriot"
          break
          ;;
  esac
done

